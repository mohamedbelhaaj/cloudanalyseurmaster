pipeline {
    agent any

    environment {
        SONAR_HOST_URL = 'http://host.docker.internal:9000'
        DOCKER_IMAGE = 'mohamedbelhaj/angular-app'
        DOCKER_TAG = "${BUILD_NUMBER}"
        DOCKERHUB_CREDENTIALS = credentials('dockerhub-credentials')
        SONAR_AVAILABLE = 'true'  // mettre true si Sonar doit être exécuté
    }

    stages {

        stage('Checkout') {
            steps {
                git url: 'https://github.com/mohamedbelhaaj/cloudanalyseurmaster.git', branch: 'main'
            }
        }

        stage('SonarQube Analysis') {
            when {
                expression { env.SONAR_AVAILABLE == 'true' }
            }
            steps {
                script {
                    def scannerHome = tool 'SonarScanner'
                    withCredentials([string(credentialsId: 'sonarscanner', variable: 'SONAR_TOKEN')]) {
                        sh """
                            echo "===== Running SonarScanner for Angular ====="
                            ${scannerHome}/bin/sonar-scanner \
                                -Dsonar.projectKey=angular-app \
                                -Dsonar.projectName=angular-app \
                                -Dsonar.sources=src \
                                -Dsonar.host.url=${SONAR_HOST_URL} \
                                -Dsonar.token=${SONAR_TOKEN} \
                                -Dsonar.exclusions=**/*.spec.ts,**/node_modules/**,**/dist/**,**/*.test.ts \
                                -Dsonar.sourceEncoding=UTF-8 \
                                -Dsonar.typescript.lcov.reportPaths=coverage/lcov.info
                            echo "===== SonarQube analysis completed ====="
                        """
                    }
                }
            }
        }

        stage('Quality Gate') {
            when {
                expression { env.SONAR_AVAILABLE == 'true' }
            }
            steps {
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: false
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                sh """
                    echo "===== Building Docker Image ====="
                    docker build -t ${DOCKER_IMAGE}:${DOCKER_TAG} .
                    docker tag ${DOCKER_IMAGE}:${DOCKER_TAG} ${DOCKER_IMAGE}:latest
                    echo "===== Docker Image created: ${DOCKER_IMAGE}:${DOCKER_TAG} ====="
                    docker images | grep angular-app
                """
            }
        }

        stage('Push to Docker Hub') {
            steps {
                script {
                    sh """
                        echo "===== Logging in to Docker Hub ====="
                        echo ${DOCKERHUB_CREDENTIALS_PSW} | docker login -u ${DOCKERHUB_CREDENTIALS_USR} --password-stdin
                        
                        echo "===== Pushing Docker Image ====="
                        docker push ${DOCKER_IMAGE}:${DOCKER_TAG}
                        docker push ${DOCKER_IMAGE}:latest
                        
                        echo "===== Push completed successfully ====="
                        docker logout
                    """
                }
            }
        }

    }

    post {
        always {
            echo '🏁 Pipeline terminé.'
        }
        success {
            echo '✅ Build et déploiement réussis!'
        }
        unstable {
            echo '⚠️ Build instable (SonarQube Quality Gate échoué)'
        }
        failure {
            echo '❌ Build échoué!'
        }
        cleanup {
            cleanWs()
        }
    }
}
